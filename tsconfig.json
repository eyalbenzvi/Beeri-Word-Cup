{
  "_comment": "TypeScript configuration. The .ts/.tsx migration is complete (allowJs:false). Strict sub-flags that the codebase ALREADY satisfies (0 tsc errors) are enabled now: noImplicitThis, strictFunctionTypes, strictBindCallApply, alwaysStrict. The two big remaining tightenings are deferred (each is large + best done off a live tournament): noImplicitAny (~715 errors) and strictNullChecks (~342). Enable them incrementally, each as its own commit, then flip the `strict` umbrella.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "allowJs": false,
    "checkJs": false,
    "strict": false,
    "noImplicitAny": false,
    "strictNullChecks": false,
    "noImplicitThis": true,
    "strictFunctionTypes": true,
    "strictBindCallApply": true,
    "alwaysStrict": true,
    "noEmit": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "types": ["@testing-library/jest-dom"],
    "verbatimModuleSyntax": false,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*", "tests/**/*"],
  "exclude": ["node_modules", "dist", ".netlify", "tests/**/*.mjs"]
}
